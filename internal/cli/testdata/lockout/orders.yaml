apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: orders
spec:
  clusterRef:
    name: lockout
  topicName: payments.orders
  partitions: 1
  access:
    producers:
      - principal: "User:svc-checkout"
        operations:
          - Write
