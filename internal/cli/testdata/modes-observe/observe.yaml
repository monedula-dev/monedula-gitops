apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: observe
spec:
  clusterRef:
    name: modes-obs
  topicName: modes.observe
  partitions: 3
  config:
    retention.ms: "604800000"
  reconciliation:
    mode: ObserveOnly
