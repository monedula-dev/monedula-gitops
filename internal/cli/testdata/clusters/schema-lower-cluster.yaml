apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaCluster
metadata:
  name: schl
  annotations:
    gitops.monedula.dev/mock-state-file: schema-lower-state.yaml
    gitops.monedula.dev/mock-schema-file: schema-lower-sr.yaml
spec:
  bootstrapServers: localhost:9092
  schemaRegistry:
    endpoint: http://sr
