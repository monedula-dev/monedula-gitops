# Topic already exists with the manifest's shape (no kafka drift). The only
# desired change is the compatibility convergence on the custom subject.
topics:
  - name: payments.orders
    partitions: 1
