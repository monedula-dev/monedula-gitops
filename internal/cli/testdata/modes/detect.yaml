apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: detect
spec:
  clusterRef:
    name: modes
  topicName: modes.detect
  partitions: 3
  config:
    retention.ms: "604800000"
  reconciliation:
    mode: DetectOnly
