# Fixture for read-failure injection: ListACLs is denied, everything else works.
topics:
  - name: payments.orders
    partitions: 3
failures:
  - method: ListACLs
    error: "describing acls: CLUSTER_AUTHORIZATION_FAILED: Cluster authorization failed."
