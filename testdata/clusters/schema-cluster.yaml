apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaCluster
metadata:
  name: sch
  annotations:
    gitops.monedula.dev/mock-state-file: schema-state.yaml
    gitops.monedula.dev/mock-schema-file: schema-sr.yaml
spec:
  bootstrapServers: localhost:9092
  schemaRegistry:
    endpoint: http://sr
