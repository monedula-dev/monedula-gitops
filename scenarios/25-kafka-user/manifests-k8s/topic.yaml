apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: users-orders
spec:
  clusterRef:
    name: shared
  topicName: users.orders
  partitions: 1
  access:
    producers:
      - principal: "User:svc-orders-app"
        operations:
          - Write
