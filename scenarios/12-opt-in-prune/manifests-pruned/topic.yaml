apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: prune-demo
spec:
  clusterRef:
    name: shared
  topicName: prune.demo
  partitions: 1
  access:
    producers:
      - principal: "User:svc-prune"
        operations:
          - Write
