apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: diff-demo
spec:
  clusterRef:
    name: shared
  topicName: diff.demo
  partitions: 1
  config:
    cleanup.policy: compact
