apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: delete-demo
  annotations:
    gitops.monedula.dev/allow-delete: "true"
spec:
  clusterRef:
    name: shared
  topicName: delete.demo
  partitions: 1
  deletionPolicy: Delete
  config:
    cleanup.policy: compact
