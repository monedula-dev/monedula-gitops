# Schema Registry HTTP Basic auth users (DEMO ONLY -- fake credentials).
# Format:  <username>: <password>,<role>[,<role>...]
# The role must match SCHEMA_REGISTRY_AUTHENTICATION_ROLES (admin).
sr: sr-secret,admin
