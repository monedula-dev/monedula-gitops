apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: observe-demo
spec:
  clusterRef:
    name: shared
  topicName: observe.demo
  partitions: 1
  config:
    cleanup.policy: compact
  reconciliation:
    mode: ObserveOnly
