steps:
  - run: apply
    manifests: manifests-enforce
    expect: { exitCode: 0 }
  - run: mutate
    mutate:
      topicConfig: { topic: observe.demo, set: { cleanup.policy: delete } }
  - run: verify
    expect: { exitCode: 0, outputContains: ["observe.demo"] }
