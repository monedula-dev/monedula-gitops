apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: drift-demo
spec:
  clusterRef:
    name: shared
  topicName: drift.demo
  partitions: 1
  config:
    cleanup.policy: compact
