apiVersion: gitops.monedula.dev/v1alpha1
kind: KafkaTopic
metadata:
  name: oauth-demo
spec:
  clusterRef:
    name: shared
  topicName: oauth.demo
  partitions: 1
  config:
    cleanup.policy: compact
